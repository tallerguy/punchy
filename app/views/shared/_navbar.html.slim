nav.navbar.navbar-expand-lg.navbar-light.bg-light
  .container
    = link_to 'Punchy', authenticated_root_path, class: 'navbar-brand'
    button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#topNav" aria-controls="topNav" aria-expanded="false" aria-label="Toggle navigation"
      span class="navbar-toggler-icon"
    #topNav.collapse.navbar-collapse
      ul.navbar-nav
        li.nav-item = link_to 'Punches', punches_path, class: 'nav-link'
        li.nav-item = link_to 'Lock Screen', lock_users_passcodes_path, method: :delete, class: 'nav-link', data: { toggle: "popover", trigger: "hover", content: "Temporary logout. Login with pin" }
      ul.nav.navbar-nav.ml-auto
        li.dropdown
          a class="nav-link dropdown-toggle" href="#" id="navbarDropdown" role="button" data-toggle="dropdown" aria-haspopup="true" aria-expanded="false"
            span.mr-2 = truncate(current_user.name, length: 25, separator: ' ')
            span.caret
          .dropdown-menu aria-labelledby="navbarDropdown"
            = link_to edit_profile_path, class: 'dropdown-item'
              i.fa.fa-user-circle-o.mr-1
              | Profile
            = link_to destroy_user_session_path, method: :delete, class: 'dropdown-item' do
              i.fa.fa-sign-out.mr-1
              | Logout
